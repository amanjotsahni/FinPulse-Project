
name: 'dbt_finpulse'
version: '1.0.0'
profile: 'dbt_finpulse'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_finpulse:
    silver:
      +materialized: view
    gold:
      +materialized: table
  elementary:
    +schema: elementary